/**
 * Created by Bullsized on 25-Jul-17.
 */
//args is for arguments, can put anything we want in that field: parameters, digits, nums, etc...

function sum(args) {
    let firstNum = Number(args[0]);
    let secondNum = Number(args[1]);

    console.log(firstNum + secondNum);
}

//sum(['10', '20']);
//sum(['66', '4']);